%Load in the data to x, y x = examples, y = outcomes
load cleandata_students.mat;
%put through ANNdata transforms data 
[x2, y2] = ANNdata(x, y);
%PART 1 done

%Lets split the data to create the 1 output neaural networks
%I have no idea if this is what you actually do....
